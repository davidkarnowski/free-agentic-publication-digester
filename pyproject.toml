[project]
name = "fapd"
version = "0.1.0"
description = "Free Agentic Publication Digester (FAPD): automated, cited, opinion-agnostic daily digest of official US government publications"
readme = "README.md"
license = "Apache-2.0"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "David D. Karnowski" }]
requires-python = ">=3.12"
keywords = ["fapd", "government", "digest", "govinfo", "civic-tech", "ai-agents", "provenance"]
dependencies = [
    "requests>=2.32",
    "python-dotenv>=1.0",
    "pypdf>=6.14.2",
    "pillow>=12.3.0",
    "markdown>=3.6",
    "pyyaml>=6.0.3",
    "protego>=0.6.2",
    "dkimpy>=1.1.8",
    "anthropic>=0.66",
]

[project.urls]
Homepage = "https://fapd.info"
Repository = "https://github.com/davidkarnowski/free-agentic-publication-digester"
Issues = "https://github.com/davidkarnowski/free-agentic-publication-digester/issues"

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fapd"]

[tool.pytest.ini_options]
# Bare `pytest` collects only the real suite. Without this, the dev
# stack's staged build context (deploy/dev/repo/ — a full working-tree
# copy including tests/) is collected too, and every test module exists
# twice under one import name: 29 collection errors.
testpaths = ["tests"]

[tool.ruff]
line-length = 100
