# Copy to ~/.fapd-deploy.env (NOT into the repo) and fill in. deploy.sh
# sources it; the repository itself carries no server coordinates.
SSH_KEY="$HOME/path/to/ssh_key"
VPS="user@host"
PORT="22"
REMOTE_DIR="/opt/fapd"
