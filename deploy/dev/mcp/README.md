# Dev-stack MCP manifest

Placeholder. A copy of `deploy/vps/mcp/fapd.manifest.json` that adds
`localhost` to `allowed_hosts` for `http://localhost:8080/mcp` lands here
in Phase 4B of `docs/ops/plan-2026-09-13-agent-discovery.md`.
