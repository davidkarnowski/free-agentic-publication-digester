# Documentation audit — 2026-08-24

*Read-only audit of the prose against the code, covering the eighteen
commits `11ab4a5..14137fc` (2026-08-15 → 08-20) that landed with no
WORKLOG entry and no CLAUDE.md §14 decision: the Gemini backend and its
retry commits, the sources activity graph, TTS/audio, static-asset
copying. Baseline: `uv run pytest -q` → 702 passed; ruff clean;
SOURCES.md/registry drift tests pass. Fix list is P5 of
[ops/plan-2026-08-24-eod-llm-resilience.md](ops/plan-2026-08-24-eod-llm-resilience.md).
Nothing here has been applied.*

Format: **doc → claim → code → fix.** Most consequential first within
each file.

## CLAUDE.md

1. **§3 stack table, LLM row** → "pluggable backends: `claude` CLI /
   Anthropic API (`LLM_BACKEND=api`)" → `src/fapd/llm.py:207-295`
   `GeminiBackend` (`LLM_BACKEND in ("gemini","google")`, key from
   `GOOGLE_GEMINI_API_KEY`/`GEMINI_API_KEY`/`GOOGLE_API_KEY`),
   `config.py:118-140` `LLM_MODELS["gemini"]` (both tiers →
   `gemini-2.5-flash`). Production has run `gemini` since 2026-08-15 after
   the CLI backend was disabled 2026-08-14 → add the Google backend and
   the cutover date.
2. **§3 "exactly one script"** → `publish.py:1405` and `:1436` emit an
   inline `onclick="…a.play()…"` handler per audio player; `site/about.html`
   carries five. The `<script`-tag audits (`tests/test_publish.py:840`,
   `docs/agents/publication.md:107`) do not see attribute handlers →
   remove the handlers (recommended) or amend the rule everywhere it is
   stated (code-standards r10, privacy.md, about.md, README:333, OB-18).
3. **§3 "650+ tests"** and **README.md:37 "665 (verified 2026-08-07)"**
   → 702 collected → update both.
4. **§3 deps** → matches `pyproject.toml` (correct). But two new external
   vendors — OpenAI via `urllib` in `tts.py`, Google via `requests` in
   `llm.py` — appear nowhere in the stack table.
5. **§9 `RenderWorker._refresh_health` "rewrites `site/sources*.html`"**
   → since `fc7ab66`, `refresh_sources` also writes `style.css`
   (`publish.py:3033`) → add "and `style.css`".
6. **§12 table** → no row for `src/fapd/tts.py`; `compose.py:131-136`
   writes `site/assets/audio/digest-<date>.mp3` from the compose layer
   (editorial code writing into the site tree) → add a row.
7. **§14** → missing decisions: Gemini backend + production cutover
   (2026-08-15), TTS/audio (2026-08-18), static-asset copy + image bake
   (2026-08-20).
8. **§5 DB claims** → verified correct against `db.py:367-369`,
   `client.py:88`, `llm.py:300`.

## GUIDE.md

9. **§6 r7 (~line 1155)** → "the `claude` CLI remains the default … an
   Anthropic-API backend serves hosted runs" → hosted runs use Gemini
   (`llm.py:307`). The 2026-08-05 amendment requires failover to be
   "explicit and logged"; the switch was an `.env` edit via
   `scripts/staged/2026-08-15-switch-to-gemini-backend.sh` with no
   governing-doc trace → name Google as a live provider, record the
   cutover, and note that no automatic failover mechanism exists.
10. **§3a inventory (~line 718, "Seven prompt surfaces"; ~761 "Media
    model surfaces … not yet built")** → TTS narration (`tts.py`, OpenAI
    `tts-1`) is a model-generated output with no version constant, no
    ledger row, no gate → admit a versioned/ledgered "narration"
    surface or state that the feature is gated off and why (operator
    ruling, plan P5 item 2).
11. **§4 (~line 970) "`fetch_log.db` … written by the HTTP client itself
    so nothing can bypass it"** → `tts.py:80-99` (`urllib` →
    api.openai.com) and `llm.py:223` (`requests.post` →
    generativelanguage.googleapis.com) bypass it. LLM calls are ledgered
    in `llm_ledger.db` by design; TTS calls are logged nowhere → scope
    the sentence to publisher fetches; add a TTS ledger or a stated
    exemption.
12. **§3 (~line 590) "We do not mirror audio or video"** → about agency
    media; nothing covers self-generated audio, and ~40 MB of MP3 is now
    committed under `site/assets/audio/` (`7628614`, `14137fc` — a feature
    commit that also carried `site/` evidence, contrary to CLAUDE.md §8).

## docs/code-standards.md

13. **§2 r10 (lines 83-92)** → same as item 2; r10 also requires the
    public privacy claims to change "in the same commit" — they did not.
14. **§1 seams table (line 25)** → lists `CLIBackend(runner=)` /
    `AnthropicBackend(client=)` → add `GeminiBackend(api_key=,
    requester=)` (`llm.py:216`); note `OpenAITTSService` has no
    injectable transport (`test_tts.py` patches `urllib`).

## docs/agents/*.md

15. **publication.md:80** → "`build_today` does not write `style.css`
    (only `build_site` does)" → `refresh_sources` writes it
    (`publish.py:3033`) → "only `build_site` and `refresh_sources` do".
16. **publication.md:55-60, 107** → "One script exists … Do not add JS";
    the `git grep "<script"` audit passes but is blind to `onclick` →
    widen to `grep -E "<script|on[a-z]+="`. The `git grep -n "llm"
    src/fapd/report.py` audit still finds no call sites but matches a
    data key (`:2176-2177`) and `re.fullmatch` (`:2211`) → add a
    `| grep -v fullmatch` note.
17. **acquisition.md:121-124** → audit `grep -rn "requests.get\|requests.post"
    src/fapd/ | grep -v client.py` → "only hits inside the client" now
    **fails**: `llm.py:223`. `git grep -i "api_key ="` → "only env reads"
    now also hits `tts.py:43`, `llm.py:217` (constructor assignments) →
    add the LLM/TTS exemption; note `tts.py` uses `urllib` and evades the
    audit entirely.
18. **orchestration.md ownership matrix (31-33)** → `src/fapd/tts.py`
    owned by no section; publication does not list `site/assets/` or
    `static_assets/` → assign.
19. **editorial.md:33 and backlog "CLI-backend zero-token failures"** →
    CLI is not the production backend; document Gemini's transient
    classes (429/500/502/503/504 → `TransientLLMError`, `llm.py:258`) and
    that `_TRANSIENT_RETRIES = 1` (`llm.py:86`) with **no backoff** — the
    `a4058cc` "exponential backoff" was removed in `3e88577`; a doc must
    not parrot the commit title.
20. **No section doc** mentions the 7-day activity heatmap / polling
    micro-bars (`health.py:493`, `:783-883`; `publish.py` timeline CSS);
    GUIDE §3a "source surfaces" covers only the two prose layers → add to
    publication.md's source-page bullets.

## deploy/vps/README.md, Dockerfile.backend, docs/continuous-ingestion.md

21. **Dockerfile.backend:10-13 comment** → "The claude CLI
    (LLM_BACKEND=cli …) … Node 22" — installed for a backend that can no
    longer authenticate on the box → say it is retained as fallback, or
    drop the install (operator call, plan P6).
22. **deploy/vps/README.md** → no mention of server-side `.env` keys
    (`LLM_BACKEND`, `GOOGLE_GEMINI_API_KEY`, `OPENAI_API_KEY`/`TTS_*`),
    of the `static_assets` bake (`Dockerfile.backend:24`), or of
    `build_site`'s asset-copy order (`publish.py:3059-3072`, size-compare
    not hash-compare) → one "server-side `.env` keys" paragraph pointing
    at `.env.example`.
23. **continuous-ingestion.md §9 (164-175)** → image "python:3.12-slim +
    uv + git" (omits Node/CLI/curl/openssh); volumes `fapd-data`,
    `fapd-site` only (omits `fapd-digests`, `fapd-provenance`, 2026-08-07);
    "durable `finalized` marker in `collector_state.last_result`" → it is
    the `finalized_date` column (`db.py:284`, since 2026-08-02). Stale
    since before this range; real.

## docs/site/* (public pages)

24. **accessibility.md:54-60** → "daily digests … feature synthesized
    audio narrations" → only `about.html` has players; no digest page has
    audio (digest TTS fires only with `OPENAI_API_KEY`, which production
    does not set); the committed site holds an orphan
    `digest-2026-07-23.mp3` (a retired day) and six
    `blog-gemini-guest-*.mp3` (14 MB) that no page references;
    `publish.render_audio_heading` (`:1398`) has zero callers → correct
    the claim; keep-and-wire or remove the orphans (operator ruling).
25. **privacy.md:12-24, about.md:95-96** → "exactly one small inline
    script" / "no scripts" → see item 2 ("no external scripts" remains
    true).
26. **`.env.example:44-45`** → shows `FAPD_COMPOSE_MODEL_GEMINI=gemini-2.5-pro`
    as the example, reading as the default; the default is flash
    (`config.py:136/140`) → reword.

## docs/pre-publication-todo.md, docs/ops/ops-backlog.md

27. **todo:282-299 "Redundant, provider-segmented inference"** → still
    `[ ]`; the provider half shipped (Gemini). Still true: no
    Protocol/ABC for backends; an unrecognized `LLM_BACKEND` silently
    falls through to the CLI (`llm.py:311`) → mark partially done, keep
    the two defects open.
28. **OB-18 (backlog:308-323)** → "any change that introduces a second
    script … should not land before this does"; inline `onclick` breaks
    a hash-only `script-src` → annotate: trigger fired 2026-08-18.
29. **todo:323 multi-media research** → unaffected (TTS is not
    ingestion); cross-reference so the two are not conflated.
30. **OB-12 stale-output cleanup** → the asset copier (`publish.py:3059`)
    adds another never-deleted class (`out_dir/assets/**`).

## Code comment drift found on the way

31. **`collect.py:542-543` `EODWorker` docstring** → "per UTC day
    at/after `config.EOD_UTC_HOUR`" → the constant is `EOD_ET_HOUR` on
    the Eastern clock.
32. **`collect.py` HALT message** → "run scripts/run_pipeline.py --date D
    … the next success clears the ladder" → a manual `run_pipeline.py`
    neither sets `finalized_date` nor pushes evidence (plan P2 item 3).

## Verified correct (so the next audit can skip them)

CLAUDE.md §5 DB pragmas; §3 deps vs `pyproject.toml`; §8 evidence paths;
OPS-GUIDE and `fapd-health` / `fapd-run` skill commands (every script
and flag exists; the ledger query already groups by `backend`, so Gemini
spend is visible); corpus audit (no `DROP TABLE/COLUMN` in `src/`);
operations audit (no compose call in `collect.py`); editorial audit
(`llm\.|LLMClient` absent from report/publish); `db.py` and
`docs/schema.md` unchanged in the range; deploy README's "four volumes"
matches `docker-compose.yml`; GUIDE §6 r7's "ledger records backend +
resolved model" honored by `GeminiBackend` (`test_llm.py:369`).
