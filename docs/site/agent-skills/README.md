# Agent Skills sources

Placeholder. Four `<name>/SKILL.md` files (Agent Skills Discovery RFC
v0.2.0) land here in Phase 1 of `docs/ops/plan-2026-09-13-agent-discovery.md`
(AD-6). `publish._doc_sources` globs `docs/site/*.md` non-recursively,
so nothing in this directory renders as a site page; the build copies
the SKILL.md files to `site/.well-known/agent-skills/`.
