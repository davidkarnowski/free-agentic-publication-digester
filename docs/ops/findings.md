# Findings register

*Stable-ID register of defects and risks found in reviews, sweeps, and
incidents. IDs are immutable and sequential (`F-001`, `F-002`, …);
severity is a column, not part of the ID. **Update `Status` in place —
never delete a row**; resolved findings keep their history. New
findings get the next ID.*

| ID | Sev | Area | Finding | Status |
|---|---|---|---|---|
| F-001 | low | repo hygiene | `.claude/` was wholly gitignored, so skills could not be tracked; split into `.claude/*` + `!.claude/skills/` (settings.local.json stays local — it's a permission allowlist). | resolved 2026-07-30 (standards push) |
| F-002 | info | deploy | `docker compose up -d` does not recreate a container whose only change is a single-file bind mount's content (new inode after rsync) — stage-B config silently not applied on first deploy. Convention: `--force-recreate <service>` for config-only deploys. | resolved 2026-07-30 (recorded in servicing guide §3 and the cohabitant bundle's README) |
| F-003 | low | corroboration | ~180 captures from 2026-07-28 lack a Wayback second witness; 31/37 Wayback submissions blocked on 2026-07-30 (their throttling). | open → tracked as OB-5 |
| F-004 | high | deploy | deploy.sh's bundle rsync ran `--delete` without excluding the box-only paths — it deleted the VPS `.env` on first real run (the deploy key survived only because root-owned `secrets/` was unreadable, which also aborted further deletions). Excludes added and marked load-bearing; `.env` re-provisioned. The sibling project's guide warned about exactly this. | resolved 2026-07-30 |
| F-005 | low | collect | First-boot race: parallel workers each attempt the fetch-log `client`-column ALTER on a fresh DB — losers get `duplicate column name` (contained, self-heals next cycle). Guard the ALTER with a caught exception. | open |
| F-006 | med | collect | The EOD pause stops *new* collector cycles but does not drain in-flight ones — the finalizer overlaps whatever was already running (safe under WAL/budgets, but the docs §7 serialization promise is only half-kept). Add a busy-flag drain wait. | open |
| F-007 | low | politeness | Each worker cycle constructs a fresh AgencyClient, so robots.txt is re-fetched per host per cycle (~half the agency-class request spend). Cache robots per worker or reuse the client across cycles. | resolved 2026-07-31 — cached in `robots_cache` (fetch_log.db) with the existing 24h TTL, so it survives both a new client and a container restart; temporary 5xx/network disallows are deliberately NOT persisted, since they describe a moment rather than a host. Removing this waste was the precondition for the hourly cadence and the agency budget raise. |
| F-008 | high | deploy | The baked repo's origin remote is HTTPS, so container evidence pushes cannot authenticate — accidentally safe for the experimental phase (no push can clobber canonical history), but OB-11 must flip it to SSH deliberately, together with state seeding, before pushes are enabled for real. | resolved 2026-07-30 (deploy.sh re-flips origin to SSH post-up every deploy; deploy-key auth verified with ls-remote; see OB-11 Done-note) |
| F-009 | low | deploy | Named-volume seeding is first-mount-only: rebuilding the backend image does not refresh the fapd-site volume, so presentation changes deployed mid-day stayed invisible until an in-container build_site ran. deploy.sh now rebuilds the site in-container post-up. | resolved 2026-07-30 |
| F-010 | med | analyze/tokens | The VPS's first-day backlog grind burned ~4M input tokens in map-layer retries: 6-item batches persistently returned fewer summaries than requested (likely CLI output truncation on dense judicial/bill text), and every shortfall escalated through the group→single retry ladder. It converged (0 pending for 07-29/07-30), but the map layer had NO shortfall logging (the plain layer did), so the miss pattern was invisible until read from the ledger. Shortfall + unmatched-key logging added to both layers 2026-07-30; if logs confirm truncation, the next lever is a smaller map batch size or an explicit max-token bump on the CLI backend. | open — instrumented 2026-07-30; three EODs have run since — next review reads the production ledger for shortfall lines (VPS access required) |
| F-011 | high | collect/EOD | The three-clock incident (2026-08-01): `digest.default_date()` still used the UTC day, `EODWorker.cycle()`'s idle return erased the durable `finalized` marker, and `_run_finalizer()` passed no `--date` — a premature 08-01 digest published at 22:39 ET and the full pipeline re-fired every ~20 minutes (35 automated evidence commits). Full plan: docs/ops/plan-2026-08-01-eod-timing-fix.md. | resolved 2026-08-02 (`fe8e402`, deployed same day; loop verified dead — one firing, durable marker, digest superseded itself). Remaining half tracked as review D5: the error path still replaces `last_result`. |
| F-012 | low | dev stack | The staged build contexts baked `.claude/worktrees/` (recursive repo copies left by harness agents) into images — `repo-excludes.txt` excluded only `settings.local.json`. Found by the 2026-08-02 doc audit. | resolved 2026-08-02 (exclude added to deploy/common/repo-excludes.txt) |
| F-013 | high | publication | The Congressional Record is absent from every frozen digest. govinfo publishes a day's issue 8–22h after the day ends; the midnight-ET freeze precedes it (CREC-2026-08-04, 62 granules, first seen 08-05T11:42Z vs an 04:47Z freeze; CREC-2026-08-03, 154 granules, first seen 08-04T12:27Z vs 04:06Z). Since automatic EOD finalization began 2026-08-02, every session-day digest has published `Total issue size: 0 granule(s)` and a `CREC \| 0 \| 0 \| 0 \| 0 \| 0` coverage row — which reads as "Congress was inactive". The only digest with a populated §1 is 2026-07-30, rendered ~22h late by hand. | resolved 2026-08-06 pending tonight's EOD proof — observation-day filing (GUIDE §3 amended; `packages.digest_day`; 33-site query seam; three-clock renderings). CREC-2026-08-05 (87 granules) verified filed under 2026-08-06 in production; the supersession mechanism was never built and its CREC case is retired |
| F-014 | med | ops/insight | Three defects in the insight loop, found 2026-08-05. (1) `run()` calls `suggest()` before `write_text()` and `suggest()` catches only `ValueError`, so an `LLMError` loses the whole mechanical report — `insight-2026-08-03.md` was never written; the docstring already promises the opposite. (2) `gather()` windows on `time.gmtime()` today, but EOD runs ~04:00–05:00 UTC, so it measures ~5h of a 24h day: for digest 08-04 it reported 1,117 requests / 26 calls / 956,741 input tokens against a true 3,231 / 82 / 2,443,574 — 35% of requests and 39% of tokens (measured against production 2026-08-05 with the implemented Eastern-day window). (3) `llm_errors` filters `error IS NOT NULL`, so zero-billed failures are invisible — the 08-04 report said "No LLM call errors" while 15 had occurred. | resolved 2026-08-05 (all three: survive-the-call, Eastern work window, zero-billed section) — verified in production by insight-2026-08-05.md: full-day window, "None — every call in the window billed tokens", report written |
| F-015 | med | publication | The rule registry is split: `rules.py` defines 14 (`RULES` 9 + `EXCLUSIONS` 5) while `report.py:41-70` defines 19 reader-facing descriptions. Six ids that already appear in published digests — `AGENCYPR-SEL-01`, `AGENCYPR-EX-01`, `VOTES-SEL-01`, `VOTES-EX-01`, `BILLACTIONS-SEL-01`, and (added 2026-08-06) `PRESACT-EX-01` — exist only as hardcoded strings in the renderer, with no definition in `rules.py`. The count grew because the gap has no guard: `PRESACT-SEL-01`–`04` were defined properly in `rules.py` while the exclusion beside them was not, and nothing failed. Any fix should add a test that every id the renderer can emit resolves in `rules.py`, or this becomes seven. Also flagged by the 2026-08-02 code review. | open |
| F-016 | low | publication | Day-in-Review prose states observed counts as fact ("Six federal district court opinions were issued", 2026-08-04). Six were *observed at freeze*; USCOURTS arrives for 4–5 days after issue — opinions issued 08-03 gained 331 further first-sightings on 08-05, after that digest froze. The §5 standing disclosure covers the lag qualitatively but the composed prose contradicts it. | resolved 2026-08-06 — compose prompt v4 states counts as observations, and observation-day filing makes the digest day and the observation window the same thing |
| F-017 | med | security | fail2ban's `sshd` jail matches `_SYSTEMD_UNIT=sshd.service` but the unit is `ssh.service` (1,145 journal entries vs 1 over 7 days); the jail reports `Total failed: 0` while scanning continues. Bounded by key-only auth — lost defense-in-depth and lost visibility, not an open door. | open → tracked as OB-13 |
| F-020 | high | deploy | The F-008 remediation does not survive a container recreate. `deploy.sh:66` re-flips the origin remote to SSH with `docker exec … git -C /app remote set-url` — a write to the *running container's* layer, not the image, which still bakes the laptop tree's HTTPS remote. Any `docker compose up -d --force-recreate backend` reverts it and silently breaks evidence pushes. Demonstrated 2026-08-05: the OAuth token rotation recreated the backend at 00:51 and `git push --dry-run` then failed with `could not read Username for 'https://github.com'`. Fetch still worked (public repo, anonymous HTTPS), so nothing looked wrong until the push was actually tested. Structural fix: set the remote in `Dockerfile.backend` after `COPY repo/ /app` so it is baked, leaving deploy.sh's re-flip as belt-and-braces. | **resolved 2026-08-07** — done exactly as written. The defect was confirmed first rather than assumed: the running container reported the SSH remote while `docker run --rm --entrypoint git fapd-backend:latest -C /app remote -v` reported HTTPS, so the image had never carried the fix at all — every container since the stack went up depended on a post-up exec. `safe.directory` is set in the same layer and before the `git -C /app` call; a `||` fallback covers a build context whose .git has no origin. deploy.sh keeps its re-flip as belt-and-braces, and five drift guards in `tests/test_evidence_commit.py` pin the baked remote, its position after `COPY repo/ /app`, the safe.directory ordering, that the two copies of the URL cannot diverge, and that no script writes an HTTPS remote. Scope note: the dev compose builds this Dockerfile, so dev containers now carry the SSH remote too — harmless and arguably safer, having no deploy key and therefore no ability to push; `test_dev_stack_never_reflips_the_origin` was re-scoped to claim only what it checks. |
| F-019 | high | deploy | `evidence-commit.sh` has no divergence handling: line 40 is a bare `git push origin main` with no fetch, pull, or rebase, and `set -e` aborts the script on rejection. Any commit pushed to origin between an image build and an EOD run makes the evidence push a non-fast-forward and it fails. Hit 2026-08-05: the container sat at `0aabba1` while origin was at `503f853` (two commits pushed after the deploy). Consequence is asymmetric and easy to miss — the digest still renders, validates, and appears on fapd.info, because the site is served from the volume; only the git provenance record is lost. May also increment the `finalize_attempts` ladder. Fix: fetch and `--ff-only` (or rebase onto origin) before the push, with an explicit failure message on non-trivial divergence. | **resolved 2026-08-07** — and it recurred first, exactly as written: left open after the 2026-08-05 hand-fix, it silently ate the 2026-08-06 evidence commit (168 files, incl. an insight report no re-render reproduces). `evidence-commit.sh` now exports the deploy key, fetches before staging, rebases `--autostash` onto origin, and verifies HEAD == origin/main before reporting success; eight textual drift guards in `tests/test_evidence_commit.py`. The lesson is in F-021: a known-open high finding with a hand-fix and no durable alarm is a scheduled recurrence. |
| F-018 | low | deps | CVE-2026-15308 (`html.parser.HTMLParser` CPU DoS, CVSS 8.7) has no fixed CPython release on any branch as of 2026-08-05. Four subclassed call sites take fetched agency HTML (`agencies.py:744`, `probe.py:34`, `provenance.py:39`, `parsers/crec.py:53`), and our exposure is wider than the advisory implies: `client.py` sets a timeout on the *fetch* but there is no body-size cap and no wall-clock guard on the *parse*, so one malformed page stalls a collector worker indefinitely. Availability-only, and input comes from pinned first-party `.gov` hosts. No version fix exists — the lever is a guard at the call sites. | open |
| F-021 | high | ops/EOD | **What made F-019 invisible for thirteen hours.** The push failure was recorded only in `collector_state.last_result` — the field CLAUDE.md §9 already names as the wrong home for a durable fact — so the `eod` row read `finalize_date` set, `finalize_attempts=0`: a clean success by every durable measure the system kept, while fapd.info served a digest the repository did not have. It could not self-heal either: `eod_due()` returns None once `finalized_date` is set, so the next attempt was the *next day's* EOD, which fails identically, forever, until a deploy. And the near-miss underneath both: `/app` is the image, not a volume (mounts were exactly `/app/data`, `/app/site`, `/app/secrets`), so `digests/`, `provenance/` and `.git` sat in the container's writable layer — any `docker compose build` would have destroyed the day outright. Four independent safeguards would each have caught this alone; none existed. | resolved 2026-08-07 — P0 recovered the stranded commit (staged script, backup branch + host copy, rebase, push); P2 added `evidence_pushed_at` / `evidence_push_error` / `evidence_push_attempts` beside the finalize ladder with a loud `logger.error` and a bounded retry; P3 made `digests/` and `provenance/` named volumes (cost disclosed as OB-19); P5 put the check in OPS-GUIDE. `finalized_date` stays written on push failure on purpose — the day IS finalized, and re-finalizing re-spends tokens. |
| F-022 | med | publication | Every CREC granule listed on `/today` and the frozen day views showed the package id as its title — 155 rows on 2026-08-07 all reading `CREC-2026-08-06`, one distinct label for a whole day of the Congressional Record. Two defects compounding. `parsers/crec.py::_clean_title` returns None for **every** granule: each `<title>` in the GPO ZIP is byte-identical issue boilerplate, so the regex strips it to empty (1,836 CREC rows in production with a NULL title — the only collection with any missing; AGENCYPR, BILLS, FR and USCOURTS are all at zero). Then `_today_item_row` fell through to `package_id`, which for a granule-level collection is the whole day's issue. The real headings were never missing: the govinfo granules API supplies them and `granules.title` has held them all along (458/458 populated), which `report.py` section 1 has joined since the beginning — the two surfaces had quietly disagreed about what a document is called. | resolved 2026-08-07 — `today_status` COALESCEs `e.title` with `g.title` so today.json carries the official heading rather than null; `_today_display_title` reuses the digest's chain and helpers, ending at `granule_id` and reaching `package_id` only for whole-package documents. Follow-on: `_display_title` uppercased the first *character*, so `"SAM"`→`"sam"` and `O'ROURKE`→`O'rourke` — latent because the digest only ever fed it floor-debate headings; fixed with `_recase_word`. |
| F-023 | high | ops/EOD | **A provider outage halted ten finalizers.** The `claude` CLI backend was disabled by the vendor on 2026-08-14 23:08Z ("Your organization has disabled Claude subscription access for Claude Code"); production moved to Gemini's free tier on 08-15, whose quota (`generate_content_free_tier_requests, limit: 20`, reset 07:00Z) refused the 04:00Z finalizer on eight of the next ten nights. Four defects compounded: `GeminiBackend.complete` made one HTTP call and `LLMClient` retried once with no delay, ignoring the server's "retry in Ns" (a4058cc's backoff was removed by 3e88577); `run_pipeline.stage_analyze` had no catch, so one `LLMError` was exit 1 before render; the EOD ladder's spacing was the generic 10/20/40-minute error doubling, so all three attempts were spent by 06:12Z; and the daytime analyze worker drew on the same daily quota the finalizer needed. Eight digests were regenerated by hand the following afternoon. The render itself never needed a provider — `report.render` works from stored rows and the coverage gate reconciles arithmetic only. | resolved 2026-08-24 (`arch/mechanical-digest`, GUIDE §6 r15): the mechanical digest is the floor — every layer attempted and recorded in `day_inference`, a provider that is disabled/unauthenticated/out of quota trips a per-run breaker, transient errors get a bounded ladder honoring the retry hint, the finalizer's own ladder becomes `(15, 60, 200)` minutes over four attempts, and the digest's Inference row states only that no inference was available (operator ruling: never the cause). Production returned to the CLI backend at 22:28Z the same day. Deploy pending. |
| F-024 | high | ops/EOD | **Manual runs did not push — and said nothing.** `scripts/run_pipeline.py` has never invoked the evidence commit or set `finalized_date`; only `EODWorker.cycle` does. The HALT message told the operator to run exactly that script and promised "the next success clears the ladder", which a manual run cannot do. Nine hand-rendered days (2026-08-15..23) sat unpushed while the `eod` row read clean — `_retry_pending_push` only retried a push that had *failed* (error set), never one that had *not run* (`finalized_date` set, `evidence_pushed_at` NULL, error NULL). The first nightly success on 08-24 swept ten days into one commit (`492ef05`). The F-021 silence, one layer up. | resolved 2026-08-24 (`arch/mechanical-digest`): `scripts/collect.py --finalize D` runs the same EOD cycle (finalizer, durable marker, push) for a named day, bypassing the halt; the marker only moves forward; the HALT message names it; the push retry covers the never-ran shape; OPS-GUIDE and `/fapd-health` read `evidence_pushed_at`, not the absence of an error. Deploy pending. |
