# Findings register

*Stable-ID register of defects and risks found in reviews, sweeps, and
incidents. IDs are immutable and sequential (`F-001`, `F-002`, …);
severity is a column, not part of the ID. **Update `Status` in place —
never delete a row**; resolved findings keep their history. New
findings get the next ID.*

| ID | Sev | Area | Finding | Status |
|---|---|---|---|---|
| F-001 | low | repo hygiene | `.claude/` was wholly gitignored, so skills could not be tracked; split into `.claude/*` + `!.claude/skills/` (settings.local.json stays local — it's a permission allowlist). | resolved 2026-07-30 (standards push) |
| F-002 | info | deploy | `docker compose up -d` does not recreate a container whose only change is a single-file bind mount's content (new inode after rsync) — stage-B config silently not applied on first deploy. Convention: `--force-recreate <service>` for config-only deploys. | resolved 2026-07-30 (recorded in servicing guide §3 and the cohabitant bundle's README) |
| F-003 | low | corroboration | ~180 captures from 2026-07-28 lack a Wayback second witness; 31/37 Wayback submissions blocked on 2026-07-30 (their throttling). | open → tracked as OB-5 |
| F-004 | high | deploy | deploy.sh's bundle rsync ran `--delete` without excluding the box-only paths — it deleted the VPS `.env` on first real run (the deploy key survived only because root-owned `secrets/` was unreadable, which also aborted further deletions). Excludes added and marked load-bearing; `.env` re-provisioned. The sibling project's guide warned about exactly this. | resolved 2026-07-30 |
| F-005 | low | collect | First-boot race: parallel workers each attempt the fetch-log `client`-column ALTER on a fresh DB — losers get `duplicate column name` (contained, self-heals next cycle). Guard the ALTER with a caught exception. | open |
| F-006 | med | collect | The EOD pause stops *new* collector cycles but does not drain in-flight ones — the finalizer overlaps whatever was already running (safe under WAL/budgets, but the docs §7 serialization promise is only half-kept). Add a busy-flag drain wait. | open |
| F-007 | low | politeness | Each worker cycle constructs a fresh AgencyClient, so robots.txt is re-fetched per host per cycle (~half the agency-class request spend). Cache robots per worker or reuse the client across cycles. | resolved 2026-07-31 — cached in `robots_cache` (fetch_log.db) with the existing 24h TTL, so it survives both a new client and a container restart; temporary 5xx/network disallows are deliberately NOT persisted, since they describe a moment rather than a host. Removing this waste was the precondition for the hourly cadence and the agency budget raise. |
| F-008 | high | deploy | The baked repo's origin remote is HTTPS, so container evidence pushes cannot authenticate — accidentally safe for the experimental phase (no push can clobber canonical history), but OB-11 must flip it to SSH deliberately, together with state seeding, before pushes are enabled for real. | resolved 2026-07-30 (deploy.sh re-flips origin to SSH post-up every deploy; deploy-key auth verified with ls-remote; see OB-11 Done-note) |
| F-009 | low | deploy | Named-volume seeding is first-mount-only: rebuilding the backend image does not refresh the fapd-site volume, so presentation changes deployed mid-day stayed invisible until an in-container build_site ran. deploy.sh now rebuilds the site in-container post-up. | resolved 2026-07-30 |
| F-010 | med | analyze/tokens | The VPS's first-day backlog grind burned ~4M input tokens in map-layer retries: 6-item batches persistently returned fewer summaries than requested (likely CLI output truncation on dense judicial/bill text), and every shortfall escalated through the group→single retry ladder. It converged (0 pending for 07-29/07-30), but the map layer had NO shortfall logging (the plain layer did), so the miss pattern was invisible until read from the ledger. Shortfall + unmatched-key logging added to both layers 2026-07-30; if logs confirm truncation, the next lever is a smaller map batch size or an explicit max-token bump on the CLI backend. | open — instrumented 2026-07-30; three EODs have run since — next review reads the production ledger for shortfall lines (VPS access required) |
| F-011 | high | collect/EOD | The three-clock incident (2026-08-01): `digest.default_date()` still used the UTC day, `EODWorker.cycle()`'s idle return erased the durable `finalized` marker, and `_run_finalizer()` passed no `--date` — a premature 08-01 digest published at 22:39 ET and the full pipeline re-fired every ~20 minutes (35 automated evidence commits). Full plan: docs/ops/plan-2026-08-01-eod-timing-fix.md. | resolved 2026-08-02 (`fe8e402`, deployed same day; loop verified dead — one firing, durable marker, digest superseded itself). Remaining half tracked as review D5: the error path still replaces `last_result`. |
| F-012 | low | dev stack | The staged build contexts baked `.claude/worktrees/` (recursive repo copies left by harness agents) into images — `repo-excludes.txt` excluded only `settings.local.json`. Found by the 2026-08-02 doc audit. | resolved 2026-08-02 (exclude added to deploy/common/repo-excludes.txt) |
| F-013 | high | publication | The Congressional Record is absent from every frozen digest. govinfo publishes a day's issue 8–22h after the day ends; the midnight-ET freeze precedes it (CREC-2026-08-04, 62 granules, first seen 08-05T11:42Z vs an 04:47Z freeze; CREC-2026-08-03, 154 granules, first seen 08-04T12:27Z vs 04:06Z). Since automatic EOD finalization began 2026-08-02, every session-day digest has published `Total issue size: 0 granule(s)` and a `CREC \| 0 \| 0 \| 0 \| 0 \| 0` coverage row — which reads as "Congress was inactive". The only digest with a populated §1 is 2026-07-30, rendered ~22h late by hand. | open — GUIDE §5 amended 2026-08-05 (supersession, operator); disclosure floor lands first, mechanism needs a plan-task doc |
| F-014 | med | ops/insight | Three defects in the insight loop, found 2026-08-05. (1) `run()` calls `suggest()` before `write_text()` and `suggest()` catches only `ValueError`, so an `LLMError` loses the whole mechanical report — `insight-2026-08-03.md` was never written; the docstring already promises the opposite. (2) `gather()` windows on `time.gmtime()` today, but EOD runs ~04:00–05:00 UTC, so it measures ~5h of a 24h day: for digest 08-04 it reported 1,117 requests / 26 calls / 956,741 input tokens against a true 3,231 / 82 / 2,443,574 — 35% of requests and 39% of tokens (measured against production 2026-08-05 with the implemented Eastern-day window). (3) `llm_errors` filters `error IS NOT NULL`, so zero-billed failures are invisible — the 08-04 report said "No LLM call errors" while 15 had occurred. | open |
| F-015 | med | publication | The rule registry is split: `rules.py` defines 14 (`RULES` 9 + `EXCLUSIONS` 5) while `report.py:41-70` defines 19 reader-facing descriptions. Five ids that already appear in published digests — `AGENCYPR-SEL-01`, `AGENCYPR-EX-01`, `VOTES-SEL-01`, `VOTES-EX-01`, `BILLACTIONS-SEL-01` — exist only as hardcoded strings in the renderer, with no definition in `rules.py`. Also flagged by the 2026-08-02 code review. | open |
| F-016 | low | publication | Day-in-Review prose states observed counts as fact ("Six federal district court opinions were issued", 2026-08-04). Six were *observed at freeze*; USCOURTS arrives for 4–5 days after issue — opinions issued 08-03 gained 331 further first-sightings on 08-05, after that digest froze. The §5 standing disclosure covers the lag qualitatively but the composed prose contradicts it. | open |
| F-017 | med | security | fail2ban's `sshd` jail matches `_SYSTEMD_UNIT=sshd.service` but the unit is `ssh.service` (1,145 journal entries vs 1 over 7 days); the jail reports `Total failed: 0` while scanning continues. Bounded by key-only auth — lost defense-in-depth and lost visibility, not an open door. | open → tracked as OB-13 |
| F-018 | low | deps | CVE-2026-15308 (`html.parser.HTMLParser` CPU DoS, CVSS 8.7) has no fixed CPython release on any branch as of 2026-08-05. Four subclassed call sites take fetched agency HTML (`agencies.py:744`, `probe.py:34`, `provenance.py:39`, `parsers/crec.py:53`), and our exposure is wider than the advisory implies: `client.py` sets a timeout on the *fetch* but there is no body-size cap and no wall-clock guard on the *parse*, so one malformed page stalls a collector worker indefinitely. Availability-only, and input comes from pinned first-party `.gov` hosts. No version fix exists — the lever is a guard at the call sites. | open |
